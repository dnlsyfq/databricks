# databricks

```
to_date(mainleg_shipments.VTTK_ETD,'yyyyMMdd') >= '2017-01-01' 
(year(to_date(vbak.ERDAT,'yyyyMMdd')) BETWEEN '2021' AND year(curdate())) AND 




```

```
-- ALTER VIEW hive_metastore.default.handling_units AS
-- WHERE SpPI = '4001'
-- SET TBLPROPERTIES ('Material' = 'LCL', 'IntHU' = 1 )

-- ALTER TABLE hive_metastore.kl_lct.handling_unit 
-- WHERE SpPI = '4001'
-- SET TBLPROPERTIES ('Material' = 'LCL', 'IntHU' = 1 )

```
